[project]
name = "forecast-ladder"
version = "0.1.0"
description = "A fixed-protocol comparison of forecasting methods, from a seasonal naive floor up to a time-series foundation model, on real retail demand data."
requires-python = ">=3.10"
authors = [{ name = "Kinlo Ephriam Tangiri" }]
readme = "README.md"
license = { file = "LICENSE" }

# Reading the results needs almost nothing, because the forecasts are committed. The
# forecasting stack is an extra, not a dependency: see requirements.txt for why.
dependencies = [
  "pandas>=2.0",
  "numpy>=1.24",
  "pyarrow>=14.0",
  "matplotlib>=3.7",
]

[project.optional-dependencies]
app = ["streamlit>=1.40", "altair>=5.0"]
rungs = [
  "torch>=2.0",
  "statsforecast>=1.7",
  "mlforecast>=0.12",
  "lightgbm>=4.0",
  "neuralforecast>=1.7",
  "datasetsforecast>=0.0.8",
  "chronos-forecasting",
]
dev = ["pytest>=7.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
